import {
  Category,
  Library,
  defineConfig,
} from "@low-code/engine-shared/package-setup";

export default defineConfig({
  name: "plat-page",
  library: Library.Vue,
  category: Category.Layout,
});
